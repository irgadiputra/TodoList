<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Transaction Notification</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f6f6f6;
      margin: 0;
      padding: 0;
    }

    .container {
      max-width: 600px;
      margin: 40px auto;
      background-color: #ffffff;
      padding: 30px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.05);
    }

    h2 {
      color: #333333;
    }

    p {
      font-size: 16px;
      line-height: 1.5;
      color: #555555;
    }

    .status {
      font-weight: bold;
      padding: 10px;
      border-radius: 5px;
      text-align: center;
      margin: 20px 0;
    }

    .accepted {
      background-color: #d4edda;
      color: #155724;
    }

    .rejected {
      background-color: #f8d7da;
      color: #721c24;
    }

    .details {
      margin-top: 20px;
      background-color: #f1f1f1;
      padding: 15px;
      border-radius: 5px;
    }

    .footer {
      margin-top: 30px;
      font-size: 12px;
      color: #999999;
      text-align: center;
    }
  </style>
</head>

<body>
  <div class="container">
    <h2>Transaction Status Update</h2>
    <p>Hi {{user_name}},</p>
    <p>Your transaction for the event <strong>{{event_name}}</strong> has been processed.</p>

    <div class="status {{#if (eq status 'DONE')}}accepted{{else}}rejected{{/if}}">
      {{#if (eq status 'DONE')}}✅ Payment Accepted{{else}}❌ Payment Rejected{{/if}}
    </div>

    <div class="details">
      <p><strong>Transaction ID:</strong> {{id}}</p>
      <p><strong>Quantity:</strong> {{quantity}}</p>
      <p><strong>Total Price:</strong> Rp {{total_price}}</p>
      {{#if discounted_amount}}
        <p><strong>Discounted Price:</strong> Rp {{discounted_amount}}</p>
      {{/if}}
      <p><strong>Reward Points:</strong> {{point_reward}}</p>
    </div>

    <p>If you have any questions or believe this is a mistake, please contact our support team.</p>

    <div class="footer">
      <p>Thank you for using Loket Kita.</p>
    </div>
  </div>
</body>

</html>
